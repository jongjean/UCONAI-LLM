[
    {
        "name": "restart_system",
        "description": "Restores or restarts a specific UCONAI system component.",
        "parameters": {
            "type": "object",
            "properties": {
                "system_id": {
                    "type": "string",
                    "description": "The ID of the system (e.g., uconai-bridge, dcp-control-plane)."
                }
            },
            "required": [
                "system_id"
            ]
        }
    },
    {
        "name": "execute_powershell",
        "description": "Executes a safe PowerShell command for system maintenance.",
        "parameters": {
            "type": "object",
            "properties": {
                "command": {
                    "type": "string",
                    "description": "The PowerShell command to run."
                }
            },
            "required": [
                "command"
            ]
        }
    },
    {
        "name": "check_all_health",
        "description": "Retrieves the current status of all 11 empire systems.",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    }
]