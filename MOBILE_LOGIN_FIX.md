# 모바일 로그인 상태창 1줄 유지 - 2025-12-27

## 🎯 수정 내용

햄버거 버튼 전환 시 "회원가입 | 로그인"이 **1줄로 유지**되도록 수정

## ✅ 변경 사항

### 이전 (세로 배열)
```
회원가입
로그인
```

### 수정 후 (가로 배열)
```
회원가입 | 로그인  [☰]
```

## 📱 반응형 스타일 (900px 이하)

### CSS 수정
```css
/* 로그인 전 - 1줄 유지 */
.user-status-logged-out {
    flex-direction: row;        /* column → row */
    gap: 6px;
    white-space: nowrap;        /* 줄바꿈 방지 */
}

/* 구분선 표시 */
.status-divider {
    display: inline;            /* none → inline */
    font-size: 0.75rem;
}

/* 로그인 후 - 1줄 유지 */
.user-status-logged-in {
    flex-direction: row;        /* column → row */
    gap: 6px;
    align-items: center;        /* flex-end → center */
    white-space: nowrap;        /* 줄바꿈 방지 */
}
```

## 🎨 레이아웃

### 데스크톱 (900px 초과)
```
┌─────────────────────────────────────────────────────┐
│ 한국ESG학회    [메뉴들...]    회원가입 | 로그인     │
└─────────────────────────────────────────────────────┘
```

### 모바일 (900px 이하)
```
┌─────────────────────────────────────────┐
│ 한국ESG학회    회원가입 | 로그인  [☰] │
└─────────────────────────────────────────┘
```

### 로그인 후 모바일
```
┌───────────────────────────────────────────────────┐
│ 한국ESG학회   👤홍길동님 [마이] [로그아웃]  [☰] │
└───────────────────────────────────────────────────┘
```

## 🔧 주요 변경

| 속성 | 이전 | 수정 후 |
|------|------|---------|
| **flex-direction** | column | **row** |
| **구분선 (&#124;)** | display: none | **display: inline** |
| **white-space** | 없음 | **nowrap** |
| **align-items** | flex-end | **center** |
| **top** | 10px | **15px** |

## 📂 수정 파일
- `css/style.css` (900px 미디어 쿼리 부분)

## 🚀 배포
**Publish 탭**에서 다운로드!

---

**작성일**: 2025-12-27  
**상태**: ✅ 완료 - 모바일에서도 1줄 유지!
